    import XCTest
    @testable import MyButton2

    final class MyButton2Tests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(MyButton2().text, "Hello, World!")
        }
    }
